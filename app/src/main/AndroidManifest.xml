<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.USB_PERMISSION" />
    <uses-permission android:name="android.permission.USB_HOST" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="true" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".DayVaultStatusActivity"
            android:exported="false" />
        <activity
            android:name=".ActivityLogActivity"
            android:exported="false" />
        <activity
            android:name=".MainVaultStatusActivity"
            android:exported="false" />
        <activity
            android:name=".OverrideListActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ManualOverrideActivity"
            android:exported="false" />
        <activity
            android:name=".FingerprintEnrollmentActivity"
            android:exported="false" />
        <activity
            android:name=".DeleteUserActivity"
            android:exported="false" />
        <activity
            android:name=".SystemSettingsActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ActivateVaultActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMenuActivity"
            android:exported="true"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- USB service for fingerprint scanner hardware -->
        <service
            android:name="com.supremainc.sfm_sdk.UsbService"
            android:enabled="true"
            android:exported="false" />

        <!-- Auto-sync service for fingerprint enrollment SignalR events -->
        <service
            android:name=".service.FingerprintAutoSyncService"
            android:enabled="true"
            android:exported="false" />

        <!-- Key switch monitoring service for manual override profiles -->
        <service
            android:name=".service.KeySwitchMonitoringService"
            android:enabled="true"
            android:exported="false" />

        <!-- FileProvider for sharing downloaded APK files with the installer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- Boot Receiver to auto-start app on device boot (kiosk mode) -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>